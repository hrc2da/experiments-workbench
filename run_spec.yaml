# This file defines keys and types for a run spec
experiment_description:
  type: str
experiment_type:
  type: Object
agent:
  type: Object
# Set these if experiment type is agent
environment:
  type: Object
n_steps:
  type: int
agent_params:
  type: dict
environment_params:
  type: dict
random_seed:
  type: int
